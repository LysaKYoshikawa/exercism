const input = ['ABC', '123']
function transpose (input){

  console.log(input.splice(0,1))
  

  
}
transpose(input);



// console.log(input.map((_,letter) => input.map(word => word[letter] ? word[letter] : ' ' )));

  // console.log(input.map((_,letter) => input.map(word => {
  //   if (word[letter]) {
  //     return word[letter]
  //   } else {
  //     return ' '
  //   } 
  // }
  // )));
